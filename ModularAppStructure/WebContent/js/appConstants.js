(function (window) {
    "use strict";

    jQuery.sap.declare('js.appConstants');

    window.appConstants = {
        CONSTANT_ONE : "My Constant 1 Value",
        CONSTANT_TWO : "My Constant 2 Value"
    };
}(window));
